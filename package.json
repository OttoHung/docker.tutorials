{
  "author": "OttoHung",
  "description": "This tutorial shows up what Docker can do on building an image",
  "devDependencies": {
    "eslint": "latest",
    "eslint-config-standard": "latest",
    "eslint-plugin-react": "latest",
    "jest": "^26.0.0",
    "tsconfig-paths": "latest",
    "ts-jest": "^26.0.0",
    "ts-node": "latest",
    "typescript": "latest",
    "@ottohung/ts-clean": "latest",
    "@types/jest": "^26.0.0",
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "@types/node": "latest"
  },
  "license": "MIT",
  "name": "docker.tutorial",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "ts-clean -r workspaces/**/lib workspaces/**/dist",
    "clean:install": "ts-clean -i",
    "fix": "eslint --fix",
    "greeting": "yarn workspace @ottohung/greeting",
    "lint": "eslint",
    "test": "yarn workspaces run test"
  },
  "workspaces": [
    "workspaces/*"
  ],
  "version": "0.1.0"
}
